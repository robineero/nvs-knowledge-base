# Website

### Useful commands

Start dev server, remove directory in PowerShell, build project into /build to deploy.

~~~
npm run start
rmdir -r "path"
npm run build
~~~

https://docusaurus.io/docs/api/docusaurus-config#noIndex
